/**
 * Rol de texto dentro del slide para UI transitoria (énfasis, toolbars, migración gradual).
 * No reemplaza `SlideCanvasElementKind` del dominio.
 */
export type SlideTextRole = "title" | "subtitle" | "body";
