export const DEFAULT_IMAGE_WIDTH_PERCENT = 40;
export const DEFAULT_PANEL_HEIGHT_PERCENT = 85;
export const MAX_SLIDES_UNDO = 50;

/** Sesión de un solo uso: estado del mazo antes de `window.open` → ventana `/#/presenter` (web, sin Tauri). */
export const PRESENTER_STATE_ONE_SHOT_SESSION_KEY = "slaim-presenter-one-shot";
